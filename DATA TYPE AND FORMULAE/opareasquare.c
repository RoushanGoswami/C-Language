#include <stdio.h>
#include <conio.h>

void main()
// WAP to Find area of square

{
    float a;                                // declaration of variable a
    printf("Enter the value of side a = "); // print the written value
    scanf("%f", &a);
    // final answer will be the area of square
    printf("Area of square = %f", a * a);
}