#include <stdio.h>

void main()
{
    // 2. WAP to Find max. from given 2 no. using if else

    int a = 100, b = 30;
    if (a > b)
    {
        printf(" a is max ! ");
    }
    else
    {
        printf("b is max ! ");
    }
}